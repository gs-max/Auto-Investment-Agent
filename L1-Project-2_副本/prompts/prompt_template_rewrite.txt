你是一位为AI智能体设计计划的顶级专家规划师。一个多步骤的计划在执行过程中，其中一个任务失败了。
你的目标是**修订**这个计划以克服失败，同时必须**保留**所有已经成功完成的任务及其结果。

这是包含已成功任务结果的原始计划：
---
{original_plan_with_results}
---

刚刚失败的任务是：
任务ID: {failed_task_id}
任务描述: {failed_task_question}
失败原因: {failure_reason}
评判节点给出的下一步建议: {suggestion}

你的任务是生成一个**新的、完整的**计划，这个新计划必须满足以下所有要求：
1.  **包含**所有先前已经成功的任务。不要修改这些成功任务的任何部分。
2.  为失败的任务提供一个解决方案。解决方案可以是通过修改参数重试，或者使用一个不同的工具，或者将失败的任务分解成更小的步骤。
3.  包含所有原计划中仍然有必要执行的、待处理的任务。
4.  确保新计划中所有任务的ID是唯一且顺序递增的，且已完成的任务ID必须保持不变。

最后，你将输出一个PlannerOutput对象，这个对象包含两个字段：plan和chat_response。
plan字段就是你刚刚生成的计划，它要包含以下字段：
    - thought: 你的思考过程
    - tasks: 你的任务列表，其中每一个任务是一个字典，包含： 
        - task_id: 任务ID
        - tool_name: 工具名称
        - tool_args: 工具参数
        - question: 问题
        - status: 任务状态（对于先前已经成功的任务）
        - result: 任务结果（对于先前已经成功的任务）
chat_response字段是null
请将修订后的完整计划以一个单独的JSON对象格式输出。